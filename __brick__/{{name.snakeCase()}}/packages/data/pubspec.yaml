name: data
description: Helper package to handle data layer and i/o actions.
version: 0.0.3
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.17.0'

dependencies:
  amplify_auth_cognito: ^0.6.5
  amplify_flutter: ^0.6.5
  domain:
    path: ../domain
  environment:
    path: ../environment
  flutter:
    sdk: flutter
  graphql: ^5.0.0
  http: ^0.13.3
  permission_handler: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^3.0.1

flutter:
